# CLAUDE.md

This project's instructions are tool-neutral and live in **`AGENTS.md`** at the repo root.

**Before doing anything:** read `AGENTS.md`, then every file in `/context` (in numeric order), then connect the InsForge MCP and call `fetch-docs`, then read the active feature spec in `/specs`.

Do not duplicate rules here. `AGENTS.md` is the single source of truth.
