// Reminder lead-time choices (minutes). Labels are localized via `settings.lead.<value>`.
export const REMINDER_LEAD_OPTIONS = [15, 30, 60, 120, 1440] as const;

export const DISPLAY_NAME_MAX_LENGTH = 80;
